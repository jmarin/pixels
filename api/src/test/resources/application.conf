alpakka.s3 {
    proxy {
        host = "localhost"
        port = 8000
        secure = false
    }
    aws {
        credentials {
            provider = "static"
            access-key-id = "foo"
            secret-access-key = "bar"
        }
        region {
            provider = "static"
            default-region = "us-east-1"
        }
      
    }
    path-style-access = true
}